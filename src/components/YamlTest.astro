---
import yaml from 'js-yaml';
import aboutRaw from '../data/about.yaml?raw';

const aboutData = yaml.load(aboutRaw);
console.log('✓ YAML loaded successfully:', aboutData);
---

<div class="yaml-test">
  <h2>YAML Import Test</h2>
  <p><strong>Loaded about.yaml data:</strong></p>
  <pre>{JSON.stringify(aboutData, null, 2)}</pre>
</div>

<style>
  .yaml-test {
    background: #f5f5f5;
    padding: 16px;
    border-radius: 8px;
    margin: 16px 0;
  }

  pre {
    background: #222;
    color: #0f0;
    padding: 12px;
    border-radius: 4px;
    overflow-x: auto;
    font-size: 12px;
  }
</style>
